#!/bin/bash

# docker
alias dcmp = docker-compose
dcmp -ps
dcmp logs

docker ps

# linux: ubuntu
chmod a+w -R ./
chmod +x *.sh

ps -ef

rm -rf xxx
cp
mv

netstat -ntlp
ifconfig

cat /etc/hosts

# owner and user group
ls -al ./
cat /etc/group
chown -R 999 ./
