ls -al ~/.ssh

ssh-keygen -t rsa -b 4096 -C "dingxl18@qq.com"

eval $(ssh-agent -s)
ssh-add ~/.ssh/id_rsa

cat ~/.ssh/id_rsa.pub
clip < ~/.ssh/id_rsa.pub

ssh -T git@gitee.com
ssh -T git@github.com

